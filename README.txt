ESP32-CAM PPE Detection via Streamlit
======================================

1. Install dependencies:
   pip install streamlit pyserial

2. Change the COM port inside `app.py` (default is COM3)

3. Run the app:
   streamlit run app.py

4. A browser window will open showing live PPE detection results from your ESP32-CAM.
